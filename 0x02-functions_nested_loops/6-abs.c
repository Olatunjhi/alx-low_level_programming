#include "main.h"

/**
 *_abs - To print absolute value of int.
 *@i: variable of int type.
 *
 *Return: Always 0, success.
 */

int _abs(int i)
{

if (i > 0)
{
return (1);
}
else if (i == 0)
{
return (0);
}
else
{
return (i * -1);
}
}
